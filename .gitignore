# Dependencies and build output — regenerated by `npm install` and `npm run build`.
node_modules/
dist/

# Secrets: the Gemini key, the database password and the field-encryption keys.
.env

# Generated or local-only
server/ai-runs.jsonl
scripts/wikidata-raw.json
scripts/wikidata-progress.log
*.log
.obsidian/workspace*.json

# An untouched copy of the original download, kept only for reference.
reference-original/
